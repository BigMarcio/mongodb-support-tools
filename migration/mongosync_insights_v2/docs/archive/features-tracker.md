# Archived Feature Tracker

This file preserves the previous `FEATURES.md` tracker format.

Use `CHANGELOG.md` for shipped-history tracking and `docs/features/` for active feature behavior documentation.

## Previously Tracked Future Items

- Better contrast for setup fields on setup page

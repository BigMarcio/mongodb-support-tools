# Archived Template Structure Notes

This file replaces the older `templates/README.md` summary.

Template descriptions can drift quickly as new pages are added; refer to `docs/development.md` for the current high-level template map.
